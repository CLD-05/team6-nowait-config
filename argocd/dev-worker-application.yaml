apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nowait-worker-dev
  namespace: argocd
  labels:
    team: team6
    env: dev
    app: nowait-worker
spec:
  project: nowait
  source:
    repoURL: https://github.com/CLD-05/team6-nowait-config.git
    targetRevision: main
    path: charts/nowait-worker
    helm:
      valueFiles:
        - ../../envs/dev/worker-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: nowait
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
