apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: aws-parameter-store
spec:
  provider:
    aws:
      service: ParameterStore
      region: ap-northeast-2
      # Terraform platform-addons에서 External Secrets Operator Pod에
      # EKS Pod Identity로 SSM 권한을 연결했다는 전제입니다.
      # 그래서 AccessKey/SecretKey를 Manifest에 넣지 않습니다.
