apiVersion: v1
kind: ConfigMap
metadata:
  name: nowait-app-config
data:
  SPRING_PROFILES_ACTIVE: "dev"
  SERVER_PORT: "8080"

  # 애플리케이션에서 환경변수 이름을 다르게 사용한다면
  # 아래 이름들은 백엔드 코드 설정에 맞춰 변경하면 됩니다.
  JAVA_TOOL_OPTIONS: "-XX:MaxRAMPercentage=75.0"
