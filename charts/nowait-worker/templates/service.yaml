apiVersion: v1
kind: Service
metadata:
  name: nowait-worker
  namespace: {{ .Values.namespace }}
  labels:
    app: nowait-worker
    team: team6
spec:
  type: ClusterIP
  selector:
    app: nowait-worker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
